services:
  - type: web
    name: tunnel-relay
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    plan: free
    envVars:
      - key: SECRET
        sync: false
